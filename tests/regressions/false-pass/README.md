# Permanent false-PASS reproducers

This directory is intentionally empty at C&1-C qualification: no false PASS
was found. Every confirmed false PASS must be minimized while preserving the
unexpected `cand1 PASS`, then committed here with its manifest, seed, and
generator version. It must never remain only in a fuzz report.
